const BRANDING_BUCKET = 'usaf-branding';
let currentSettings = null;
const IMAGE_FIELDS = [
  { key:'site_logo_path', label:'Site Logo', recommended:'Recommended 1024 x 1024 PNG with transparent background. Minimum 512 x 512.', folder:'logos' },
  { key:'login_logo_path', label:'Login Logo', recommended:'Recommended 680 x 420 PNG with transparent background. Minimum 420 px wide.', folder:'login' },
  { key:'dashboard_banner_path', label:'Dashboard Banner', recommended:'Recommended 1920 x 480 JPG or WebP. Keep important content centered.', folder:'banners' },
  { key:'login_background_path', label:'Login Background', recommended:'Recommended 1920 x 1080 JPG or WebP. Dark or muted images work best.', folder:'backgrounds' }
];
async function initBrandingAdmin(){ await requireAdmin(); await renderLayout('Admin - Branding'); brandingForm.addEventListener('submit', saveBranding); await loadBranding(); }
async function loadBranding(){ const {data,error}=await window.usafSupabase.from('USAF_settings').select('*').eq('id',true).maybeSingle(); if(error)return alert(error.message); currentSettings=data||{}; organization_name.value=currentSettings.organization_name||'Orders & Travel Tracker'; primary_color.value=currentSettings.primary_color||'#00308F'; secondary_color.value=currentSettings.secondary_color||'#0A2342'; accent_color.value=currentSettings.accent_color||'#C0C0C0'; renderImageUploads(); }
function imagePreview(path){ return path?`<div class="branding-preview"><img src="${path}" alt="Branding preview"></div>`:'<div class="branding-preview empty">No image set</div>'; }
function renderImageUploads(){ imageUploadList.innerHTML=IMAGE_FIELDS.map(f=>`<div class="branding-upload-card"><div class="branding-upload-head"><strong>${f.label}</strong><small>${f.recommended}</small></div>${imagePreview(currentSettings[f.key])}<div class="branding-actions"><input type="file" id="${f.key}_file" accept="image/png,image/jpeg,image/webp"><button class="btn small" type="button" data-upload="${f.key}">Upload</button><button class="btn small secondary" type="button" data-clear="${f.key}">Clear</button></div><div class="branding-path">${currentSettings[f.key]||'No path saved'}</div></div>`).join(''); document.querySelectorAll('[data-upload]').forEach(btn=>btn.addEventListener('click',()=>uploadImage(btn.dataset.upload))); document.querySelectorAll('[data-clear]').forEach(btn=>btn.addEventListener('click',()=>clearImage(btn.dataset.clear))); }
async function saveBranding(e){ e.preventDefault(); const payload={id:true,organization_name:organization_name.value.trim()||'Orders & Travel Tracker',primary_color:primary_color.value,secondary_color:secondary_color.value,accent_color:accent_color.value}; const {error}=await window.usafSupabase.from('USAF_settings').upsert(payload); if(error)return alert(error.message); alert('Branding saved. Refresh the site to see the updated colors and name.'); await loadBranding(); }
async function uploadImage(key){ const config=IMAGE_FIELDS.find(f=>f.key===key); const input=document.getElementById(`${key}_file`); const file=input.files&&input.files[0]; if(!file)return alert('Choose an image file first.'); const extension=file.name.split('.').pop().toLowerCase(); const safeName=`${config.folder}/${key}_${Date.now()}.${extension}`; const {error:uploadError}=await window.usafSupabase.storage.from(BRANDING_BUCKET).upload(safeName,file,{upsert:true,contentType:file.type}); if(uploadError)return alert(uploadError.message+'\n\nIf this is the first time using Branding uploads, run sql/usaf_branding_storage_v32.sql in Supabase first.'); const {data}=window.usafSupabase.storage.from(BRANDING_BUCKET).getPublicUrl(safeName); const {error:updateError}=await window.usafSupabase.from('USAF_settings').update({[key]:data.publicUrl}).eq('id',true); if(updateError)return alert(updateError.message); alert(`${config.label} uploaded and saved.`); await loadBranding(); }
async function clearImage(key){ const {error}=await window.usafSupabase.from('USAF_settings').update({[key]:null}).eq('id',true); if(error)return alert(error.message); await loadBranding(); }
initBrandingAdmin();
